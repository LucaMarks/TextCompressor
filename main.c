#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "mainHeader.h"

int main(void) {

    // char word[] = "Mississippi";
    // for (int i = 0; i < strlen(word); i++) {
    //     printf("[%c, %d]", word[i], (int)word[i]);
    // }

    // int i = 5;
    // int *buff = &i;
    // int **oldNum = &buff;
    //
    // int newNum = 4;
    // int *newNump = &newNum;
    //
    // free(*oldNum);
    // *oldNum = newNump;
    // printf("%d\n", *oldNum);
    getCharacters();

    return 0;
}